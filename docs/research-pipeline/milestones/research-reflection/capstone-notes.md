# Research Reflection — Capstone notes

Back to [README](README.md) · [plan-10-capstone.md](plan-10-capstone.md).

**Status:** Done (offline / fixture path).

## Verified

```bash
# Library path (unit: test_reflection_capstone.py)
run_reflection(knowledge, slug, workspace_path=..., persist=True)
research journal --competition <slug>   # projection
research reflect run -c <slug> --execution E-xxx --offline
```

- Durable `experiment_evidence` + `belief_updates` without manual hypothesize update
- Critic `rule_engine` path works without API keys
- Journal prints evidence tiers + recommended next experiment
- Claims promotable from strong beliefs
- Legacy `labpilot.reflection` / `report` / `improvement` removed (Plan 9)

## Follow-ons

- Live unattended train + reflect on a real Kaggle competition
- Optional HTML skin for Journal
- Delete offline codegen defaults when templates retire (see capabilities TODO)
