Competition: {{ competition }}
Plan: {{ plan_id }} (kind={{ plan_kind }}) hypothesis={{ hypothesis_id or "—" }}
Task: {{ task_id }} type={{ task_type }}
Problem type: {{ problem_type }}
Improve on prior: {{ improve_on_prior }}
Parent hypothesis: {{ parent_hypothesis_id or "—" }}
Technique: {{ technique or "—" }}
Technique stack: {{ technique_stack | join(", ") if technique_stack else "—" }}
Combo techniques (apply ALL in one delta): {{ combo_techniques | join(", ") if combo_techniques else "—" }}

Goal:
{{ goal }}

Task description:
{{ task_description }}

Hypothesis triad:
- observation: {{ observation }}
- reason: {{ reason }}
- prediction: {{ prediction }}
Evidence JSON: {{ evidence_json }}
Parent metrics JSON: {{ parent_metrics_json }}

Research brief / Analyze excerpt:
{{ brief_excerpt }}

Dataset profile (JSON):
```json
{{ profile_summary_json }}
```

Data inventory under data/raw (use these real paths):
{% for f in data_inventory %}- {{ f }}
{% else %}- (empty — download may still be required)
{% endfor %}

Baseline choice hints (problem type / metric only — not a code template):
```json
{{ baseline_choice_json }}
```

Allowed roots: {{ allowed_roots | join(", ") }}
Existing files (inventory):
{% for f in existing_files %}- {{ f }}
{% else %}- (none yet)
{% endfor %}

{% if prior_train_py %}
Prior best train.py (keep what works; apply the hypothesis technique(s) as a delta;
return a FULL updated pipeline/train.py that overrides this file):
```python
{{ prior_train_py }}
```
{% endif %}

Return a CodeProposal JSON with full file contents.
{% if improve_on_prior and prior_train_py %}
{% if combo_techniques %}
Keep the working prior pipeline and merge ALL combination techniques in one delta:
{% for t in combo_techniques %}- `{{ t }}`
{% endfor %}
Do not implement only one member; do not restart as an unrelated baseline.
{% else %}
Keep the working prior pipeline and merge technique `{{ technique or "the change" }}`.
Do not restart as an unrelated baseline.
{% endif %}
{% else %}
Produce a working baseline that reads the real data above and writes
workspace-root metrics.json + submission.csv.
{% endif %}
Do not reuse or refine any legacy Jinja scaffold.
