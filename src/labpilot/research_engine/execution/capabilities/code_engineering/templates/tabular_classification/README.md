# Tabular Classification Baseline

LightGBM classifier with:
- Label-encoded categoricals
- Median imputation for numerics
- Stratified K-fold cross-validation
- Out-of-fold predictions saved to `oof.csv`
